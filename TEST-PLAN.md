# AutoCraft v1.1.1 终测清单（第二轮）

> 2026-08-14 · Rein 修订（依据首轮反馈：删 T3/T4、plan 开预览、局内指令、预览=EMI 树+数量）
> 版本：`autocraft-1.1.0.jar`（build/libs/ 最新构建）
> **本轮重点**：① 修复"合成结果无效"（扫描式填充+SYNC 轮询）② 预览界面 = EMI 配方树 + 数量选择 ③ 局内配置指令

---

## 0. 环境
- [ ] 装最新 jar；日志确认加载；`logDebug=true` 打开（本轮要抓执行细节）
- [ ] 16 原木 + 8 个甘蔗 + 3 皮革 进背包（覆盖深层合成链测试）

## P0 · 核心链路（重点回归：上轮失败的场景）

| # | 操作 | 预期 |
|:--:|------|------|
| T1 | 合成台 → 摆出木稿配方（3 木板 + 2 木棍手动放）→ 按 `B` | **预览 = EMI 配方树**（物品名+数量，缩进层级）；开始 → 回合成台自动合成 → 完成 |
| T2 | **回归上轮失败**：EMI 配方屏点 A → 目标**木稿** | 预览 → 开始 → **成功做出木稿**（不再报"合成结果无效"） |
| T3 | **回归上轮失败**：EMI 配方屏点 A → 目标**箱子** | 预览（含 log→plank ×2 多批次）→ 开始 → **成功做出箱子** |
| T4 | 背包 16 原木 → `/autocraft plan minecraft:wooden_pickaxe` | 打印计划后**自动弹出预览**；开始 → 成功 |
| T5 | 缺料：`/autocraft plan minecraft:diamond_pickaxe` | 报「缺少」，不弹预览 |

## P1 · 预览界面（新功能）

| # | 操作 | 预期 |
|:--:|------|------|
| T6 | 预览中点 `×4` 数量按钮 | 树自动重算（数量 ×4，深层节点放大）；开始 → 做出 4 个 |
| T7 | 预览中点 `×16`（材料不够时） | 树中不足节点变红/灰提示，或不可行提示；不开始 |
| T8 | 预览中按 ESC / 点取消 | 回合成台，不执行，菜单不关 |
| T9 | 预览中点 `开始合成` 后 | 回到合成台且菜单仍打开，执行正常开始（setScreen 不关菜单假设验证） |

## P2 · 局内指令（替代改配置文件）

| # | 操作 | 预期 |
|:--:|------|------|
| T10 | `/autocraft delay 3` → 触发合成 | 执行节奏明显变慢（每批间隔 3 tick）但成功 |
| T11 | `/autocraft preview off` → 按 B | 跳过预览直接执行；`/autocraft preview on` 恢复 |
| T12 | `/autocraft stop`（执行中） | 立即停止，产物保留 |

## P3 · 边界

| # | 操作 | 预期 |
|:--:|------|------|
| T13 | 深层链：`/autocraft craft minecraft:bookshelf`（甘蔗→纸→书→书架） | 计划含多层中间产物，逐步做出书架 |
| T14 | 无 EMI 环境：按 B（合成桌入口） | 不崩，预览回退平面步骤列表，可执行 |
| T15 | 连续下单 2-3 次（含不同数量） | 每次独立不串台，状态复位正确 |
| T16 | 执行中 ESC 关合成台 / 执行中拿走材料 | 停止不崩，产物保留 |

## P4 · 公网服（可选）
- [ ] 无 mod 服务器上 T1/T2；`delay` 调 2-3 不被反作弊判定

---

## ⚠️ 重点观察（Rein 标注）
1. **T2/T3**：上轮"合成结果无效"的两个场景是否被修复（扫描式填充 + 无条件 RETURN + SYNC 轮询）
2. **T9**：预览 → 开始 后菜单是否保持打开（setScreen 假设）
3. **T6**：数量切换后树是否正确重算（BoM batches 缩放）
4. 失败时开 `logDebug=true`，日志 `logs/autocraft.log` 会有 `click PICKUP/PLACE/RETURN/CRAFT` + `SCAN 完成/缺料` + `SYNC 超时 + 网格=[...]` 明细——把相关行发给 Rein

## 📮 报问题三要素
1. **路径**：T 编号 / 命令 / 界面状态
2. **根因线索**：`logs/autocraft.log` 相关行
3. **现象**：截图或精确描述
